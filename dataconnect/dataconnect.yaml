specVersion: "v1"
serviceId: "vibraciones"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "vibraciones-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
